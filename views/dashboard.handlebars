
{{#if posts.length}}
<section>
  <h2>Your Posts</h2>
  <ol>
    {{#each posts as |post|}}
    <li>
      <a href="/dashboard/edit/{{post.id}}" class="edit-link">{{post.title}}</a>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}}


